# Horizon Manager Horizons

This directory is the Horizon Manager application's own management horizon corpus.
It describes the work needed to make Horizon Manager an external multi-corpus
application rather than a tool bound to one managed project.

Use:

```bash
horizon-manager --horizons-dir management/horizons state
horizon-manager --horizons-dir management/horizons doctor
```
